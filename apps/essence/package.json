{
  "name": "@nextorders/essence",
  "type": "module",
  "version": "2.0.0-alpha.2",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --port 3503",
    "preview": "PORT=3503 nuxt preview",
    "clean": "rm -rf .nuxt .output",
    "clean:modules": "rm -rf .nuxt .output node_modules",
    "typecheck": "nuxt typecheck",
    "postinstall": "nuxt prepare",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx server/db/migrate.ts",
    "db:studio": "drizzle-kit studio",
    "db:push": "drizzle-kit push",
    "db:seed": "tsx server/db/seed.ts"
  },
  "dependencies": {
    "@date-fns/tz": "catalog:integrations",
    "@nextorders/food-schema": "workspace:*",
    "@paralleldrive/cuid2": "catalog:integrations",
    "@types/pg": "catalog:",
    "dotenv": "catalog:database",
    "drizzle-orm": "catalog:database",
    "pg": "catalog:"
  },
  "devDependencies": {
    "@types/better-sqlite3": "catalog:database",
    "@types/node": "catalog:types",
    "drizzle-kit": "catalog:database",
    "nuxt": "catalog:dev",
    "tsx": "catalog:database"
  }
}
